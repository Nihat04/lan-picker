import React from "react";
import { navLink } from "./model";

const NAV_LINKS: navLink[] = [];

const Header = () => {
    return (
        <header>
            <nav></nav>
        </header>
    );
};

export default Header;
